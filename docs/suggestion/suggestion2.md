# Suggestion: mask OpenRouter token in API responses

Settings GET returns the full API key. Consider returning a masked value (e.g. `sk-or-...xxxx`) unless the user is actively editing.

**Effort:** Small
